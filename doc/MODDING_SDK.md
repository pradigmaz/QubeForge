# 📚 QubeForge Modding Wiki

Полная документация по созданию модов для QubeForge.

---

## � Содержание

1. [Введение](#1-введение)
   - [Что такое мод?](#11-что-такое-мод)
   - [Возможности Modding API](#12-возможности-modding-api)
   - [Требования](#13-требования)

2. [Быстрый старт](#2-быстрый-старт)
   - [Создание первого мода](#21-создание-первого-мода)
   - [Установка мода](#22-установка-мода)
   - [Отладка](#23-отладка)

3. [Структура мода](#3-структура-мода)
   - [Манифест](#31-манифест)
   - [Определение мода](#32-определение-мода)
   - [Жизненный цикл](#33-жизненный-цикл)

4. [Система разрешений](#4-система-разрешений)
   - [Список разрешений](#41-список-разрешений)
   - [Запрос разрешений](#42-запрос-разрешений)
   - [Проверка разрешений](#43-проверка-разрешений)

5. [API Reference](#5-api-reference)
   - [ModAPI (основной объект)](#51-modapi-основной-объект)
   - [UI API](#52-ui-api)
   - [Player API](#53-player-api)
   - [World API](#54-world-api)
   - [Config API](#55-config-api)
   - [Events API](#56-events-api)

6. [События](#6-события)
   - [Список событий](#61-список-событий)
   - [Подписка на события](#62-подписка-на-события)
   - [Приоритеты](#63-приоритеты)
   - [Отмена событий](#64-отмена-событий)

7. [Регистрация контента](#7-регистрация-контента)
   - [Блоки](#71-блоки)
   - [Предметы](#72-предметы)
   - [Рецепты](#73-рецепты)
   - [Мобы](#74-мобы)

8. [Примеры модов](#8-примеры-модов)
   - [Счётчик блоков](#81-счётчик-блоков)
   - [Индикатор здоровья](#82-индикатор-здоровья)
   - [Координаты игрока](#83-координаты-игрока)
   - [Статистика убийств](#84-статистика-убийств)

9. [Лучшие практики](#9-лучшие-практики)
   - [Производительность](#91-производительность)
   - [Совместимость](#92-совместимость)
   - [Безопасность](#93-безопасность)

10. [Устранение неполадок](#10-устранение-неполадок)
    - [Частые ошибки](#101-частые-ошибки)
    - [Отладка в консоли](#102-отладка-в-консоли)
    - [FAQ](#103-faq)

---

## 1. Введение

### 1.1 Что такое мод?

**Мод** (модификация) — это JavaScript файл, который расширяет функциональность QubeForge без изменения исходного кода игры.

Моды могут:
- Добавлять новые блоки, предметы и рецепты
- Отображать информацию на экране (HUD)
- Реагировать на игровые события
- Изменять поведение игры
- Сохранять данные между сессиями

### 1.2 Возможности Modding API

| Категория | Возможности |
|-----------|-------------|
| **UI** | HUD элементы, уведомления, кастомные интерфейсы |
| **Мир** | Чтение/изменение блоков, время суток |
| **Игрок** | Позиция, здоровье, инвентарь |
| **События** | Разрушение/установка блоков, урон, смерть мобов |
| **Данные** | Сохранение настроек в localStorage |
| **Контент** | Регистрация блоков, предметов, рецептов |

### 1.3 Требования

- Базовые знания JavaScript
- Текстовый редактор (VS Code, Notepad++ и т.д.)
- Браузер с DevTools (F12) для отладки

---

## 2. Быстрый старт

### 2.1 Создание первого мода

Создайте файл `hello-world.js`:

```javascript
QubeForge.registerMod('hello-world', {
  name: 'Hello World',
  version: '1.0.0',
  author: 'Ваше имя',
  description: 'Мой первый мод!',
  apiVersion: '1.0',
  permissions: ['ui.hud'],
  dependencies: [],
}, {
  init(api) {
    api.ui.addHUDElement('greeting', {
      position: 'top-left',
      html: '<div style="font-size: 24px; color: gold;">👋 Привет, мир!</div>'
    });
    
    api.ui.showNotification('Мод Hello World загружен!');
  },
  
  onEnable() {
    console.log('Hello World включён');
  },
  
  onDisable() {
    console.log('Hello World выключен');
  },
});
```

### 2.2 Установка мода

1. Запустите игру QubeForge
2. В главном меню нажмите кнопку **"Моды"**
3. Перетащите `.js` файл в зону загрузки или нажмите "Выбрать файл"
4. Поставьте галочку ✅ напротив мода для активации
5. Нажмите **"Применить и перезагрузить"**
6. Зайдите в игру — мод активен!

### 2.3 Отладка

Откройте консоль браузера (F12 → Console) для просмотра:
- Логов мода (`api.log()`, `api.warn()`, `api.error()`)
- Ошибок загрузки
- Сообщений системы модов `[ModLoader]`

```javascript
// В коде мода
api.log('Информация');      // [my-mod] Информация
api.warn('Предупреждение'); // [my-mod] Предупреждение
api.error('Ошибка');        // [my-mod] Ошибка
```

---

## 3. Структура мода

### 3.1 Манифест

Манифест — объект с метаданными мода:

```javascript
{
  // === ОБЯЗАТЕЛЬНЫЕ ПОЛЯ ===
  name: 'Название мода',        // Отображается в менеджере модов
  version: '1.0.0',             // Семантическое версионирование
  apiVersion: '1.0',            // Версия Modding API (не меняйте)
  permissions: ['ui.hud'],      // Требуемые разрешения
  
  // === ОПЦИОНАЛЬНЫЕ ПОЛЯ ===
  author: 'Автор',              // Имя разработчика
  description: 'Описание',      // Краткое описание функций
  dependencies: ['other-mod'],  // ID зависимых модов
}
```

#### Версионирование

Используйте [семантическое версионирование](https://semver.org/):
- `MAJOR.MINOR.PATCH` (например, `1.2.3`)
- **MAJOR** — несовместимые изменения API
- **MINOR** — новые функции с обратной совместимостью
- **PATCH** — исправления багов

### 3.2 Определение мода

Определение — объект с функциями мода:

```javascript
{
  // === ОБЯЗАТЕЛЬНАЯ ФУНКЦИЯ ===
  init(api) {
    // Вызывается при загрузке мода
    // api — объект ModAPI с доступом ко всем функциям
  },
  
  // === ОПЦИОНАЛЬНЫЕ ФУНКЦИИ ===
  onEnable() {
    // Вызывается при включении мода
  },
  
  onDisable() {
    // Вызывается при выключении мода
    // Очистите ресурсы здесь!
  },
}
```

### 3.3 Жизненный цикл

```
┌─────────────────────────────────────────────────────────┐
│                    ЗАГРУЗКА ИГРЫ                        │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│  1. ModLoader читает моды из IndexedDB                  │
│  2. Разрешение зависимостей (топологическая сортировка) │
│  3. Валидация манифестов                                │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│  4. Для каждого мода:                                   │
│     - Создание ModAPI с разрешениями                    │
│     - Вызов init(api)                                   │
│     - Вызов onEnable()                                  │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                    ИГРОВОЙ ЦИКЛ                         │
│  - Моды получают события                                │
│  - Моды обновляют UI                                    │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│  ВЫГРУЗКА (при выключении мода):                        │
│  - Вызов onDisable()                                    │
│  - Очистка подписок на события                          │
│  - Удаление HUD элементов                               │
└─────────────────────────────────────────────────────────┘
```

---

## 4. Система разрешений

### 4.1 Список разрешений

| Разрешение | Описание | Методы |
|------------|----------|--------|
| `ui.hud` | Создание HUD элементов и уведомлений | `api.ui.*` |
| `world.read` | Чтение блоков мира | `world.getBlock()`, `world.hasBlock()`, `world.getTopY()` |
| `world.modify` | Изменение блоков мира | `world.setBlock()`, `world.removeBlock()` |
| `player.read` | Чтение данных игрока | `player.getPosition()`, `player.getHp()` |
| `player.modify` | Изменение данных игрока | `player.setHp()`, `player.heal()`, `player.damage()` |
| `mobs.register` | Регистрация новых мобов | `api.registerMob()` |
| `crafting.modify` | Добавление рецептов | `api.registerRecipe()` |

### 4.2 Запрос разрешений

Укажите все необходимые разрешения в манифесте:

```javascript
permissions: [
  'ui.hud',        // Для HUD и уведомлений
  'world.read',    // Для чтения блоков
  'player.read',   // Для чтения здоровья
],
```

**Важно:** Запрашивайте только те разрешения, которые реально используете!

### 4.3 Проверка разрешений

При вызове метода без разрешения выбрасывается ошибка:

```javascript
// Если нет разрешения 'world.read':
const block = api.getWorld().getBlock(0, 0, 0);
// Error: [my-mod] Permission denied: world.read
```

---

## 5. API Reference

### 5.1 ModAPI (основной объект)

Объект `api`, передаваемый в `init()`:

```javascript
interface ModAPI {
  // Метаданные
  readonly modId: string;      // ID мода
  readonly version: string;    // Версия мода
  readonly apiVersion: string; // Версия API
  
  // Подсистемы
  ui: UIAPI;                   // UI и уведомления
  config: ConfigAPI;           // Сохранение настроек
  assets: AssetManager;        // Загрузка ресурсов
  
  // Методы
  getWorld(): WorldAPI;        // Доступ к миру
  getPlayer(): PlayerAPI;      // Доступ к игроку
  
  // События
  on(event, handler, priority?): () => void;
  once(event, handler, priority?): () => void;
  
  // Регистрация контента
  registerBlock(id, config): number;
  registerItem(id, config): number;
  registerRecipe(id, config): number;
  registerMob(id, config): number;
  
  // Утилиты
  log(message, ...args): void;
  warn(message, ...args): void;
  error(message, ...args): void;
  resolveId(stringId): number | undefined;
}
```


### 5.2 UI API

API для создания интерфейсных элементов:

```javascript
const ui = api.ui;
```

#### addHUDElement(id, options)

Добавляет элемент на экран.

```javascript
api.ui.addHUDElement('my-element', {
  position: 'top-left',  // Позиция на экране
  html: '<div>HTML контент</div>',  // HTML (санитизируется)
  // ИЛИ
  text: 'Простой текст',  // Безопасный текст
  style: {  // Опционально: CSS стили
    fontSize: '16px',
    color: 'white',
    backgroundColor: 'rgba(0,0,0,0.5)',
    padding: '10px',
  }
});
```

**Позиции:**
| Значение | Расположение |
|----------|--------------|
| `top-left` | Левый верхний угол |
| `top-right` | Правый верхний угол |
| `bottom-left` | Левый нижний угол |
| `bottom-right` | Правый нижний угол |

#### updateHUDElement(id, content)

Обновляет содержимое элемента:

```javascript
api.ui.updateHUDElement('my-element', {
  html: '<div>Новый контент</div>'
});

// Или только текст
api.ui.updateHUDElement('my-element', {
  text: 'Новый текст'
});
```

#### removeHUDElement(id)

Удаляет элемент:

```javascript
api.ui.removeHUDElement('my-element');
```

#### showNotification(message, duration?)

Показывает всплывающее уведомление:

```javascript
api.ui.showNotification('Сообщение');  // 3 секунды по умолчанию
api.ui.showNotification('Долгое сообщение', 5000);  // 5 секунд
```

---

### 5.3 Player API

API для работы с игроком:

```javascript
const player = api.getPlayer();
```

#### Чтение данных (требует `player.read`)

```javascript
// Позиция игрока
const pos = player.getPosition();
// { x: 10.5, y: 64.0, z: 20.3 }

// Здоровье
const hp = player.getHp();        // Текущее (0-20)
const maxHp = player.getMaxHp();  // Максимальное (20)

// Направление взгляда
const dir = player.getDirection();
// { x: 0.5, y: -0.2, z: 0.8 }
```

#### Изменение данных (требует `player.modify`)

```javascript
// Установить здоровье
player.setHp(10);

// Вылечить
player.heal(5);  // +5 HP (не больше maxHp)

// Нанести урон
player.damage(3);  // -3 HP
```

---

### 5.4 World API

API для работы с миром:

```javascript
const world = api.getWorld();
```

#### Чтение блоков (требует `world.read`)

```javascript
// Получить ID блока на координатах
const blockId = world.getBlock(10, 64, 20);
// 0 = воздух, 1 = трава, 2 = земля, ...

// Проверить наличие блока
const exists = world.hasBlock(10, 64, 20);
// true/false

// Найти высоту поверхности
const topY = world.getTopY(10, 20);
// Y координата верхнего блока в колонке
```

#### Изменение блоков (требует `world.modify`)

```javascript
// Установить блок
world.setBlock(10, 64, 20, 1);  // Поставить траву

// Удалить блок
world.removeBlock(10, 64, 20);  // Заменить на воздух
```

#### Время и окружение

```javascript
// Время суток (0.0 - 1.0)
const time = world.getTimeOfDay();
// 0.0 = полночь, 0.25 = рассвет, 0.5 = полдень, 0.75 = закат

// День или ночь?
const isDay = world.isDay();
// true если 0.25 < time < 0.75
```

---

### 5.5 Config API

API для сохранения настроек мода:

```javascript
const config = api.config;
```

Данные сохраняются в `localStorage` и доступны между сессиями.

#### get(key, defaultValue?)

Получить значение:

```javascript
const score = api.config.get('highScore', 0);
const name = api.config.get('playerName', 'Игрок');
const settings = api.config.get('settings', { sound: true, music: true });
```

#### set(key, value)

Сохранить значение:

```javascript
api.config.set('highScore', 1000);
api.config.set('playerName', 'Pro Player');
api.config.set('settings', { sound: true, music: false });
```

**Поддерживаемые типы:** `string`, `number`, `boolean`, `object`, `array`

#### remove(key)

Удалить значение:

```javascript
api.config.remove('highScore');
```

#### getAll()

Получить все настройки мода:

```javascript
const allSettings = api.config.getAll();
// { highScore: 1000, playerName: 'Pro Player', ... }
```

---

### 5.6 Events API

API для подписки на игровые события:

#### on(event, handler, priority?)

Подписаться на событие:

```javascript
const unsubscribe = api.on('world:blockBreak', (event) => {
  console.log('Блок разрушен:', event.data);
});

// Отписаться
unsubscribe();
```

#### once(event, handler, priority?)

Одноразовая подписка (автоматически отписывается после первого вызова):

```javascript
api.once('player:damage', (event) => {
  api.ui.showNotification('Первый урон!');
});
```

---

## 6. События

### 6.1 Список событий

| Событие | Данные | Описание |
|---------|--------|----------|
| `world:blockBreak` | `{x, y, z, blockId}` | Игрок разрушил блок |
| `world:blockPlace` | `{x, y, z, blockId}` | Игрок установил блок |
| `player:damage` | `{amount, newHp, maxHp}` | Игрок получил урон |
| `mob:death` | `{mobType, x, y, z}` | Моб был убит |

### 6.2 Подписка на события

```javascript
// Разрушение блока
api.on('world:blockBreak', (event) => {
  const { x, y, z, blockId } = event.data;
  api.log(`Блок ${blockId} разрушен на (${x}, ${y}, ${z})`);
});

// Установка блока
api.on('world:blockPlace', (event) => {
  const { x, y, z, blockId } = event.data;
  api.ui.showNotification(`Установлен блок #${blockId}`);
});

// Урон игроку
api.on('player:damage', (event) => {
  const { amount, newHp, maxHp } = event.data;
  if (newHp <= 5) {
    api.ui.showNotification('⚠️ Низкое здоровье!');
  }
});

// Смерть моба
api.on('mob:death', (event) => {
  const { mobType, x, y, z } = event.data;
  api.log(`${mobType} убит на (${x.toFixed(1)}, ${y.toFixed(1)}, ${z.toFixed(1)})`);
});
```

### 6.3 Приоритеты

Приоритет определяет порядок вызова обработчиков:

```javascript
api.on('world:blockBreak', handler, 'HIGHEST');  // Вызывается первым
api.on('world:blockBreak', handler, 'HIGH');
api.on('world:blockBreak', handler, 'NORMAL');   // По умолчанию
api.on('world:blockBreak', handler, 'LOW');
api.on('world:blockBreak', handler, 'LOWEST');   // Вызывается последним
```

### 6.4 Отмена событий

```javascript
api.on('world:blockBreak', (event) => {
  // Отменить событие (другие обработчики не получат его)
  event.cancel();
  
  // Проверить, отменено ли событие
  if (event.isCancelled()) {
    return;
  }
});
```

---

## 7. Регистрация контента

### 7.1 Блоки

```javascript
// Требует разрешение: world.modify
const blockId = api.registerBlock('my_block', {
  name: 'Мой блок',
  hardness: 2.0,        // Время добычи (секунды)
  texture: 'stone',     // Текстура (из атласа)
  drops: 'my_mod:my_block',  // Что выпадает
  tool: 'pickaxe',      // Требуемый инструмент
});
```

### 7.2 Предметы

```javascript
// Требует разрешение: world.modify
const itemId = api.registerItem('my_item', {
  name: 'Мой предмет',
  stackSize: 64,        // Максимум в стаке
  texture: 'custom',    // Текстура
});
```

### 7.3 Рецепты

```javascript
// Требует разрешение: crafting.modify
api.registerRecipe('my_recipe', {
  type: 'shaped',       // shaped или shapeless
  pattern: [
    'AAA',
    'ABA',
    'AAA'
  ],
  key: {
    'A': 'minecraft:stone',
    'B': 'minecraft:diamond',
  },
  result: {
    item: 'my_mod:my_block',
    count: 1,
  },
});
```

### 7.4 Мобы

```javascript
// Требует разрешение: mobs.register
api.registerMob('my_mob', {
  name: 'Мой моб',
  health: 20,
  damage: 5,
  speed: 1.0,
  hostile: true,
  drops: [
    { item: 'my_mod:my_item', count: [1, 3] }
  ],
});
```

---

## 8. Примеры модов

### 8.1 Счётчик блоков

Считает разрушенные блоки с сохранением между сессиями:

```javascript
QubeForge.registerMod('block-counter', {
  name: 'Счётчик блоков',
  version: '1.0.0',
  author: 'Example',
  description: 'Считает разрушенные блоки',
  apiVersion: '1.0',
  permissions: ['ui.hud'],
  dependencies: [],
}, {
  init(api) {
    let count = api.config.get('totalBlocks', 0);
    
    api.ui.addHUDElement('counter', {
      position: 'top-right',
      html: `<div style="background: rgba(0,0,0,0.7); padding: 10px; border-radius: 5px;">
        🧱 Разрушено: <strong>${count}</strong>
      </div>`
    });
    
    api.on('world:blockBreak', () => {
      count++;
      api.config.set('totalBlocks', count);
      api.ui.updateHUDElement('counter', {
        html: `<div style="background: rgba(0,0,0,0.7); padding: 10px; border-radius: 5px;">
          🧱 Разрушено: <strong>${count}</strong>
        </div>`
      });
    });
  },
  onEnable() {},
  onDisable() {},
});
```


### 8.2 Индикатор здоровья

Показывает статус здоровья с цветовой индикацией:

```javascript
QubeForge.registerMod('health-indicator', {
  name: 'Индикатор здоровья',
  version: '1.0.0',
  author: 'Example',
  description: 'Визуальный индикатор здоровья',
  apiVersion: '1.0',
  permissions: ['ui.hud', 'player.read'],
  dependencies: [],
}, {
  init(api) {
    const updateHealth = (hp, maxHp) => {
      let color, icon, status;
      const percent = (hp / maxHp) * 100;
      
      if (percent > 70) {
        color = '#4ade80'; icon = '❤️'; status = 'Отлично';
      } else if (percent > 40) {
        color = '#facc15'; icon = '💛'; status = 'Осторожно';
      } else if (percent > 20) {
        color = '#f97316'; icon = '🧡'; status = 'Опасно';
      } else {
        color = '#ef4444'; icon = '💔'; status = 'КРИТИЧНО!';
      }
      
      api.ui.updateHUDElement('health-status', {
        html: `<div style="
          background: rgba(0,0,0,0.8);
          padding: 12px 16px;
          border-radius: 8px;
          border-left: 4px solid ${color};
        ">
          <div style="color: ${color}; font-size: 18px;">
            ${icon} ${status}
          </div>
          <div style="color: #888; font-size: 12px; margin-top: 4px;">
            ${hp} / ${maxHp} HP
          </div>
        </div>`
      });
    };
    
    // Начальное состояние
    api.ui.addHUDElement('health-status', {
      position: 'bottom-left',
      html: '<div>Загрузка...</div>'
    });
    
    // Обновление при уроне
    api.on('player:damage', (event) => {
      updateHealth(event.data.newHp, event.data.maxHp);
      
      if (event.data.newHp <= 5) {
        api.ui.showNotification('⚠️ КРИТИЧЕСКОЕ ЗДОРОВЬЕ!', 2000);
      }
    });
    
    // Периодическое обновление
    setInterval(() => {
      const player = api.getPlayer();
      updateHealth(player.getHp(), player.getMaxHp());
    }, 1000);
  },
  onEnable() {},
  onDisable() {},
});
```

### 8.3 Координаты игрока

Отображает текущие координаты и направление:

```javascript
QubeForge.registerMod('coords-display', {
  name: 'Координаты',
  version: '1.0.0',
  author: 'Example',
  description: 'Показывает координаты игрока',
  apiVersion: '1.0',
  permissions: ['ui.hud', 'player.read', 'world.read'],
  dependencies: [],
}, {
  init(api) {
    api.ui.addHUDElement('coords', {
      position: 'top-left',
      html: '<div>Загрузка...</div>'
    });
    
    const getDirection = (dir) => {
      const angle = Math.atan2(dir.x, dir.z) * (180 / Math.PI);
      if (angle >= -45 && angle < 45) return 'Юг (S)';
      if (angle >= 45 && angle < 135) return 'Запад (W)';
      if (angle >= -135 && angle < -45) return 'Восток (E)';
      return 'Север (N)';
    };
    
    setInterval(() => {
      const player = api.getPlayer();
      const pos = player.getPosition();
      const dir = player.getDirection();
      const world = api.getWorld();
      
      if (!pos) return;
      
      const timePercent = Math.round(world.getTimeOfDay() * 100);
      const dayNight = world.isDay() ? '☀️ День' : '🌙 Ночь';
      
      api.ui.updateHUDElement('coords', {
        html: `<div style="
          background: rgba(0,0,0,0.75);
          padding: 12px;
          border-radius: 6px;
          font-family: monospace;
          font-size: 13px;
          line-height: 1.6;
        ">
          <div style="color: #fff;">📍 Координаты</div>
          <div style="color: #aaa;">X: <span style="color: #f87171;">${pos.x.toFixed(1)}</span></div>
          <div style="color: #aaa;">Y: <span style="color: #4ade80;">${pos.y.toFixed(1)}</span></div>
          <div style="color: #aaa;">Z: <span style="color: #60a5fa;">${pos.z.toFixed(1)}</span></div>
          <div style="color: #888; margin-top: 8px;">🧭 ${getDirection(dir)}</div>
          <div style="color: #888;">${dayNight} (${timePercent}%)</div>
        </div>`
      });
    }, 200);
  },
  onEnable() {},
  onDisable() {},
});
```

### 8.4 Статистика убийств

Отслеживает убитых мобов по типам:

```javascript
QubeForge.registerMod('kill-stats', {
  name: 'Статистика убийств',
  version: '1.0.0',
  author: 'Example',
  description: 'Отслеживает убитых мобов',
  apiVersion: '1.0',
  permissions: ['ui.hud'],
  dependencies: [],
}, {
  init(api) {
    // Загрузка сохранённой статистики
    const stats = api.config.get('killStats', {});
    
    const updateDisplay = () => {
      const entries = Object.entries(stats);
      if (entries.length === 0) {
        api.ui.updateHUDElement('kills', {
          html: '<div style="background: rgba(0,0,0,0.7); padding: 10px; border-radius: 5px; color: #888;">🎯 Нет убийств</div>'
        });
        return;
      }
      
      const rows = entries
        .sort((a, b) => b[1] - a[1])
        .map(([mob, count]) => `<div>${mob}: <strong>${count}</strong></div>`)
        .join('');
      
      api.ui.updateHUDElement('kills', {
        html: `<div style="background: rgba(0,0,0,0.7); padding: 10px; border-radius: 5px;">
          <div style="color: #fff; margin-bottom: 5px;">🎯 Убийства</div>
          <div style="color: #aaa; font-size: 12px;">${rows}</div>
        </div>`
      });
    };
    
    api.ui.addHUDElement('kills', {
      position: 'top-right',
      html: '<div>Загрузка...</div>'
    });
    
    updateDisplay();
    
    api.on('mob:death', (event) => {
      const mobType = event.data.mobType;
      stats[mobType] = (stats[mobType] || 0) + 1;
      api.config.set('killStats', stats);
      updateDisplay();
      
      api.ui.showNotification(`🎯 ${mobType} убит! (всего: ${stats[mobType]})`);
    });
  },
  onEnable() {},
  onDisable() {},
});
```

---

## 9. Лучшие практики

### 9.1 Производительность

#### ❌ Плохо: Частые обновления DOM

```javascript
// Каждый кадр — плохо!
setInterval(() => {
  api.ui.updateHUDElement('fps', { html: `FPS: ${fps}` });
}, 16);
```

#### ✅ Хорошо: Разумная частота обновления

```javascript
// Раз в 200-500мс — достаточно
setInterval(() => {
  api.ui.updateHUDElement('fps', { html: `FPS: ${fps}` });
}, 500);
```

#### ❌ Плохо: Утечки памяти

```javascript
init(api) {
  setInterval(() => { /* ... */ }, 1000);
  // Интервал никогда не очищается!
}
```

#### ✅ Хорошо: Очистка ресурсов

```javascript
init(api) {
  this.intervalId = setInterval(() => { /* ... */ }, 1000);
},
onDisable() {
  clearInterval(this.intervalId);
}
```

### 9.2 Совместимость

#### Проверяйте наличие данных

```javascript
const pos = api.getPlayer().getPosition();
if (pos) {  // Может быть null до загрузки
  // Используем pos
}
```

#### Используйте уникальные ID

```javascript
// ❌ Плохо: может конфликтовать с другими модами
api.ui.addHUDElement('counter', { ... });

// ✅ Хорошо: уникальный префикс
api.ui.addHUDElement('mymod-counter', { ... });
```

#### Не полагайтесь на порядок загрузки

```javascript
// ❌ Плохо: зависимость может не загрузиться
const otherMod = window.OtherMod;

// ✅ Хорошо: используйте dependencies в манифесте
dependencies: ['other-mod'],
```

### 9.3 Безопасность

#### HTML санитизируется автоматически

```javascript
// Опасные теги удаляются:
api.ui.addHUDElement('test', {
  html: '<script>alert("XSS")</script><div>Safe</div>'
});
// Результат: <div>Safe</div>
```

#### Не храните чувствительные данные

```javascript
// ❌ Плохо
api.config.set('password', '12345');

// ✅ Хорошо: только игровые данные
api.config.set('highScore', 1000);
```

---

## 10. Устранение неполадок

### 10.1 Частые ошибки

#### "Permission denied: ..."

**Причина:** Мод использует функцию без соответствующего разрешения.

**Решение:** Добавьте разрешение в манифест:
```javascript
permissions: ['ui.hud', 'world.read'],  // Добавьте нужное
```

#### "api.ui.addHudElement is not a function"

**Причина:** Неправильное имя метода (регистр букв).

**Решение:** Используйте `addHUDElement` (с большими HUD):
```javascript
api.ui.addHUDElement('id', { ... });  // ✅ Правильно
api.ui.addHudElement('id', { ... });  // ❌ Неправильно
```

#### "Cannot read property 'x' of null"

**Причина:** Данные ещё не загружены.

**Решение:** Проверяйте на null:
```javascript
const pos = api.getPlayer().getPosition();
if (pos) {
  console.log(pos.x);
}
```

#### Мод не появляется в списке

**Причина:** Ошибка синтаксиса JavaScript.

**Решение:** 
1. Откройте консоль (F12)
2. Найдите ошибку парсинга
3. Исправьте синтаксис

### 10.2 Отладка в консоли

```javascript
// Логирование с префиксом мода
api.log('Информация');      // [my-mod] Информация
api.warn('Предупреждение'); // [my-mod] Предупреждение  
api.error('Ошибка');        // [my-mod] Ошибка

// Отладка событий
api.on('world:blockBreak', (event) => {
  console.log('Event data:', event.data);
  console.log('Event cancelled:', event.isCancelled());
});
```

### 10.3 FAQ

**Q: Как удалить мод?**
A: Откройте меню "Моды" и нажмите 🗑️ рядом с модом.

**Q: Где хранятся моды?**
A: В IndexedDB браузера. Они сохраняются между сессиями.

**Q: Можно ли использовать TypeScript?**
A: Нет, только JavaScript. Но можно компилировать TS в JS.

**Q: Как сбросить настройки мода?**
A: Вызовите `api.config.remove('key')` для каждого ключа или очистите localStorage.

**Q: Моды работают на мобильных?**
A: Да, если браузер поддерживает IndexedDB.

**Q: Как связаться с разработчиками?**
A: Создайте issue на GitHub: https://github.com/pradigmaz/QubeForge

---

## 📝 Changelog

### API Version 1.0 (Январь 2026)

- ✅ Базовая система модов
- ✅ UI API (HUD, уведомления)
- ✅ Player API (позиция, здоровье)
- ✅ World API (блоки, время)
- ✅ Config API (сохранение настроек)
- ✅ События: blockBreak, blockPlace, player:damage, mob:death
- ✅ Система разрешений
- ✅ Менеджер модов с drag & drop

---

*Документация QubeForge Modding SDK v1.0*
