# Структура проекта Minecraft Clone

Этот документ описывает текущую архитектуру проекта и назначение каждого файла после рефакторинга.

## 📂 src/

### 📄 main.ts
**Точка входа.** Инициализирует рендерер, загружает текстуры, создает экземпляр класса `Game` и запускает основной цикл.

### 📂 core/ (Ядро движка)
- **`Game.ts`**: Центральный класс-координатор. Хранит ссылки на все системы (World, Player, UI и т.д.), управляет игровым циклом (`update`, `render`) и состоянием запуска.
- **`Renderer.ts`**: Обертка над Three.js. Настраивает сцену, камеру, освещение, рендерер WebGL и PointerLockControls. Обрабатывает ресайз окна.
- **`GameState.ts`**: Хранит глобальные флаги состояния: `isPaused`, `isGameStarted`, и ссылку на предыдущее меню.

### 📂 world/ (Мир и Окружение)
- **`World.ts`**: Управление чанками (генерация, сохранение, загрузка), хранение воксельных данных, генерация мешей чанков.
- **`Environment.ts`**: Отвечает за цикл день/ночь, солнце, луну, облака, цвет неба и тумана.

### 📂 player/ (Логика Игрока)
- **`Player.ts`**: Фасад для систем игрока. Объединяет физику, здоровье, бой и визуализацию руки.
- **`PlayerPhysics.ts`**: Обработка движения, гравитации, прыжков и коллизий игрока с блоками.
- **`PlayerHealth.ts`**: Управление здоровьем, получение урона, респаун, визуальные эффекты урона.
- **`PlayerCombat.ts`**: Логика атаки (Raycasting), нанесение урона мобам, кулдаун атаки.
- **`PlayerHand.ts`**: Визуализация предмета/блока в руке (качание при ходьбе, анимация удара).

### 📂 entities/ (Сущности)
- **`ItemEntity.ts`**: Класс для выброшенных предметов (лут), лежащих на земле. Обрабатывает физику падения и вращение.

### 📂 mobs/ (Мобы и AI)
- **`MobManager.ts`**: Спавн, обновление и деспавн мобов. Управляет глобальным списком мобов.
- **`Mob.ts`**: Базовый класс моба. Физика, AI (стейт-машина: Idle, Wander, Chase), получение урона.
- **`Zombie.ts`**: Наследник `Mob`. Модель зомби из примитивов, специфичная анимация.

### 📂 blocks/ (Взаимодействие с блоками)
- **`BlockBreaking.ts`**: Логика ломания блоков (время разрушения, текстура трещин).
- **`BlockInteraction.ts`**: Логика установки блоков (ПКМ) и взаимодействия (верстак).
- **`BlockCursor.ts`**: Визуальный курсор (wireframe куб), выделяющий блок, на который смотрит игрок.

### 📂 inventory/ (Инвентарь)
- **`Inventory.ts`**: Структура данных инвентаря. Слоты, добавление/удаление предметов, сериализация.
- **`InventoryUI.ts`**: Отрисовка интерфейса инвентаря и хотбара.
- **`DragDrop.ts`**: Логика перетаскивания предметов мышью/тачем внутри инвентаря.

### 📂 crafting/ (Крафтинг)
- **`CraftingSystem.ts`**: Логика проверки рецептов (Shaped и Shapeless), потребление ресурсов.
- **`CraftingUI.ts`**: Визуализация сетки крафта (2x2 или 3x3) и списка рецептов для мобильных устройств.
- **`Recipes.ts`**: База данных всех рецептов игры.

### 📂 ui/ (Пользовательский интерфейс)
- **`Menus.ts`**: Управление экранами (Главное меню, Пауза, Настройки). Кнопки и переходы.
- **`CLI.ts`**: Командная строка (чат) для ввода команд (например, `/give`).
- **`HealthBar.ts`**: Отрисовка сердечек здоровья.
- **`HotbarLabel.ts`**: Всплывающее название предмета при переключении слота.

### 📂 mobile/ (Мобильное управление)
- **`MobileControls.ts`**: Виртуальный джойстик, сенсорные кнопки (Jump, Attack, Place) и управление камерой свайпами.

### 📂 constants/ (Константы и настройки)
- **`GameConstants.ts`**: Физические параметры (гравитация, скорость), размеры игрока.
- **`BlockNames.ts`**: ID блоков и их строковые названия.
- **`BlockTextures.ts`**: Определения UV-координат и цветов для блоков.
- **`ToolTextures.ts`**: Генерация пиксельных текстур инструментов из ASCII-паттернов.

### 📂 utils/ (Утилиты)
- **`DB.ts`**: Работа с IndexedDB для сохранения мира (meta и chunks).
- **`BlockColors.ts`**: Хелпер для получения цветов блоков для UI.
- **`DebugUtils.ts`**: Инструменты отладки (смена времени суток и т.д.).
